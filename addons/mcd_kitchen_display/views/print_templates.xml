<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="print_modifier_tickets_template" name="Kitchen Grill Slip Print">
        <html>
            <head>
                <title>Grill Slip</title>
                <style>
                    @page { size: 80mm auto; margin: 4mm; }
                    body {
                        color: #111;
                        font-family: Arial, sans-serif;
                        font-size: 18px;
                        font-weight: 800;
                        margin: 0;
                    }
                    .ticket {
                        page-break-after: always;
                        width: 72mm;
                    }
                    .ticket:last-child {
                        page-break-after: auto;
                    }
                    .row {
                        border-bottom: 1px dashed #999;
                        padding: 10px 0;
                    }
                    .label {
                        display: block;
                        font-size: 11px;
                        letter-spacing: 1px;
                        margin-bottom: 4px;
                        text-transform: uppercase;
                    }
                    .value {
                        display: block;
                        font-size: 24px;
                        line-height: 1.25;
                    }
                    .modifier {
                        border: 2px solid #111;
                        display: block;
                        font-size: 24px;
                        line-height: 1.35;
                        margin-top: 6px;
                        padding: 12px;
                        white-space: pre-wrap;
                    }
                    .empty {
                        font-size: 15px;
                        font-weight: 700;
                        padding: 20px 0;
                        text-align: center;
                    }
                    @media print {
                        .no-print { display: none; }
                    }
                </style>
                <script>
                    window.addEventListener('load', function () {
                        setTimeout(function () { window.print(); }, 250);
                    });
                </script>
            </head>
            <body>
                <t t-if="tickets">
                    <t t-foreach="tickets" t-as="ticket">
                        <div class="ticket">
                            <div class="row">
                                <span class="label">Mã đơn</span>
                                <span class="value" t-esc="order.name"/>
                            </div>
                            <div class="row">
                                <span class="label">Mon an</span>
                                <span class="value" t-esc="ticket['product_name']"/>
                            </div>
                            <div class="row">
                                <span class="label">+ Modifier</span>
                                <span class="modifier" t-esc="ticket['modifier_note']"/>
                            </div>
                        </div>
                    </t>
                </t>
                <t t-else="">
                    <div class="ticket">
                        <div class="empty">Don nay khong co modifier.</div>
                    </div>
                </t>
            </body>
        </html>
    </template>
</odoo>
